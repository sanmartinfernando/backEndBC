import { ExecutionContext, NestInterceptor, CallHandler } from '@nestjs/common';
import { Observable } from 'rxjs';
import * as morgan from 'morgan';
export declare abstract class AbstractMorganInterceptor implements NestInterceptor {
    private morganInstance;
    protected abstract readonly options: morgan.Options;
    protected abstract readonly format: string | morgan.FormatFn;
    constructor(morganInstance: morgan.Morgan);
    intercept(context: ExecutionContext, next: CallHandler<any>): Observable<any>;
}
